import {View} from 'react-native'
import {Text} from 'react-native-paper'

export default Singup = ({navigator}) => {
    return (
        <View>
            <Text>Registro</Text>
        </View>
    )
}